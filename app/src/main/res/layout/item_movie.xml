<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/ivPoster"
        android:layout_width="120dp"
        android:layout_height="wrap_content"
        tools:srcCompat="@tools:sample/avatars" />

    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="34dp"
        android:layout_alignEnd="@+id/ivPoster"
        android:layout_marginStart="1dp"
        android:layout_marginEnd="-289dp"
        android:layout_toEndOf="@+id/ivPoster"
        android:text="TextView"
        android:textSize="24sp"
        tools:text="Fast  Furious hobs and shaw" />

    <TextView
        android:id="@+id/tvOverview"
        android:layout_width="291dp"
        android:layout_height="84dp"
        android:layout_below="@+id/ivPoster"
        android:layout_alignBottom="@+id/ivPoster"
        android:layout_marginStart="0dp"
        android:layout_marginTop="-92dp"
        android:layout_marginBottom="8dp"
        android:layout_toEndOf="@+id/ivPoster"
        android:text="TextView"
        tools:text="im haveing a hard time undersatnind why this is loading is such a stupid fucking way and ai need a lot of sample text in order to properly analyazy this issue" />
</RelativeLayout>